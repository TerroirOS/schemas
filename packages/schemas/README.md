# @terroiros/schemas

Canonical Terroir schema package.

## Exports

- Zod validators and TS types from package root:
  - `import { traceEventSchema } from "@terroiros/schemas"`
- JSON schemas by subpath:
  - `import schema from "@terroiros/schemas/trace/v1/trace-event.schema.json"`
